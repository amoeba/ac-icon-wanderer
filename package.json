{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "deploy": "npm run build && wrangler deploy",
    "r2:upload-icons": "node scripts/r2-upload.js data/icons",
    "r2:upload": "node scripts/r2-upload.js public"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  }
}
